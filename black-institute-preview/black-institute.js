(()=>{const root=document.documentElement;const button=document.querySelector('[data-theme-toggle]');const label=document.querySelector('[data-theme-label]');const saved=localStorage.getItem('ghalvera-theme');const preferred=window.matchMedia('(prefers-color-scheme: light)').matches?'light':'dark';const apply=theme=>{root.dataset.theme=theme;if(label)label.textContent=theme==='dark'?'Light':'Dark';if(button)button.setAttribute('aria-pressed',String(theme==='light'))};apply(saved||preferred);button?.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('ghalvera-theme',next);apply(next)})})();